[pytest]
testpaths = tests
python_files = test_*.py

# The guardrail tests never touch a database and the ones that do skip
# themselves, so a plain "pytest" run is always safe and always fast.
addopts = -q
